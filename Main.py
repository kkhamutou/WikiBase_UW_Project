test = [['title', 'Kack', 'Max'],
        ['search_word', '', ''],
        ['added_date', '', '']
        ]

# [filter(lambda x: x!='null' and x!=None, inner_list) for inner_list in list_value]
print(tuple(test[0]))
#print(res)